const greet = function (name) {
    console.log(`Hello, ${name}!`);
  };
  
  greet("John");
  

  //Function expressions assign an anonymous function to a variable. The function can be called using the variable name.