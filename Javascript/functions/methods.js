//Methods

const obj = {
    greet: function (name) {
      console.log(`Hello, ${name}!`);
    },
  };
  
  obj.greet("John");
  

  //Methods are functions that are defined as properties of objects. They are invoked using the object name followed by the method name.