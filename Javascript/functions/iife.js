// Immediately Invoked Function Expressions (IIFE)

(function () {
    console.log("This function is immediately invoked.");
  })();

  
  //IIFEs are self-invoking functions that are executed immediately after they are defined. They are often used to create a private scope.