//Named Function

function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("John");

  //Named functions are defined using the function keyword, followed by the function name and a parameter list. They can be called by their name.